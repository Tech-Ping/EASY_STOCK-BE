spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
logging:
  level:
    root: DEBUG
app:
  token:
    secretKey: ezezezdevdevdevtesttesttesttestezezezezezez
    expiration:
      access: 14400000
      refresh: 1382400000
